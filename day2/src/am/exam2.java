package am;

public class exam2 {
	public static void main(String[] args) {
		int score = 85;
		System.out.println((score >= 90) ? "A" : (score >= 80) ? "B" : (score >= 70) ? "C" : (score >= 60) ? "D" : "F");
//		ampm = (hour < 12) ? "오전" : "오후"
		
	}
}
